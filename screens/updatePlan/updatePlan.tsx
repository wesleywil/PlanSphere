import {
  StyleSheet,
  Text,
  View,
  ScrollView,
  TouchableHighlight,
} from "react-native";

const UpdatePlan = ({ route, navigation }: any) => {
  const { id } = route.params;
  return (
    <View>
      <Text>Update Plan - plansTitle</Text>
    </View>
  );
};

export default UpdatePlan;
